parse_version: "1.0"
schema_version: "1.0"

paths:
  project_root: "."
  data_root: "data/京剧剧本"
  pdf_cache: "artifacts/cache/pdf"
  output_cleaned: "artifacts/cleaned"
  collections_config: "configs/collections.yaml"

# 留空则处理 zip 内全部；开发阶段可只跑金样例
script_ids: []
# script_ids:
#   - "01001012"

# 批处理时限制每个 zip 的剧本数（0 = 不限制）
limit_per_zip: 0

# 默认处理的 zip（留空表示 data_root 下全部 *.zip）
zip_files: []
# zip_files:
#   - "01000000.zip"

analytics:
  num_topics: 8
  rhythm_window: 20
